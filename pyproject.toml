[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cryoemdoc"
version = "0.1.0"
description = "Inference-first cryo-EM image triage and documentation tools."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  {name = "cryoEMdoc contributors"}
]
keywords = ["cryo-em", "microscopy", "image-classification", "pytorch"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Image Processing",
]
dependencies = [
  "torch",
  "torchvision",
  "numpy",
  "pandas",
  "pillow",
  "opencv-python-headless",
  "tifffile",
]

[project.urls]
Homepage = "https://github.com/MaxwellNicholson/cryoemdoc"
Repository = "https://github.com/MaxwellNicholson/cryoemdoc"
Issues = "https://github.com/MaxwellNicholson/cryoemdoc/issues"
Documentation = "https://github.com/MaxwellNicholson/cryoemdoc/tree/main/docs"

[project.optional-dependencies]
mrc = ["mrcfile"]
dev = ["pytest"]
train = [
  "scikit-learn",
  "matplotlib",
  "seaborn",
  "tqdm",
  "openpyxl",
]

[project.scripts]
cryoemdoc = "cryoemdoc.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
cryoemdoc = [
  "py.typed",
  "artifacts/**/*.json",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "slow: tests that load full neural network weights or run model inference",
]
